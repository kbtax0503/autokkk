<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 회의 녹음(합친 기능): VOX 마이크 녹음 + 공개폴더 저장 + 부팅 자동시작 + 배터리최적화 제외 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- B2 무인 캡처: 백그라운드에서 카톡 방을 열려면 백그라운드 액티비티 실행 예외가 필요(overlay 권한) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_dialog_email"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.KakaoBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          카톡 알림 수신용 NotificationListenerService.
          BIND_NOTIFICATION_LISTENER_SERVICE 권한 + 표준 액션 intent-filter.
          공식 문서 패턴: exported=false (시스템이 알림접근 허용 후 특권 경로로 바인드).
        -->
        <service
            android:name=".KakaoListenerService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!--
          거래처 자료획득(B): 안드 "공유" 대상. 카톡 첨부를 "공유 → 카톡 브릿지"로 받아 서버 /asset 업로드.
          NoDisplay 테마로 화면 없이 받자마자 처리. */* = 사진·파일 모두 수신.
        -->
        <activity
            android:name=".ShareReceiverActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!--
          카톡 화면 접근성 트리 덤프(스파이크). com.kakao.talk 창에서만 반응(accessibility_config packageNames).
          system이 BIND_ACCESSIBILITY_SERVICE 권한으로 바인드 → exported=true(이 권한은 시스템만 보유).
        -->
        <service
            android:name=".KakaoAccessibilityService"
            android:exported="true"
            android:label="@string/a11y_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

        <!-- 회의 녹음(VOX) 포그라운드 서비스 — 마이크. meeting-recorder 합병. -->
        <service
            android:name=".VoxService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- 부팅 후 자동 시작(이전에 '녹음 켜짐'이었던 경우). -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
